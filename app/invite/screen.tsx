"use client";
import {useEffect,useState} from 'react';
export default function Invite(){const [code,setCode]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false),[success,setSuccess]=useState(false);useEffect(()=>{setCode(location.hash.slice(1))},[]);return <main className="wrap section narrow"><a href="/" className="logo">zeliy<span>.</span></a><h1 style={{marginTop:35}}>You’re invited.</h1><p>Sign in with the email your invitation was issued to, then accept below. Keep this link private.</p><p><a className="underlink" href="/signin-with-chatgpt?return_to=%2Finvite" target="_top">Sign in with ChatGPT</a></p><p>After signing in, reopen your original invitation link if its code is not shown below.</p><label>Invitation code<input style={{width:'100%',border:'1px solid #ddd',padding:12,margin:'12px 0'}} value={code} onChange={e=>setCode(e.target.value)} maxLength={64}/></label>{message&&<p role="status" className="notice">{message}</p>}<button disabled={busy||success||!code} className="primary" onClick={async()=>{setBusy(true);try{const r=await fetch('/api/access/accept',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})});const d:any=await r.json();if(!r.ok)throw Error(d.error);setSuccess(true);setMessage('Invitation accepted. Your account is ready.');history.replaceState(null,'','/invite');setCode('')}catch(e){setMessage((e as Error).message)}finally{setBusy(false)}}}>{busy?'Accepting…':'Accept invitation'}</button>{success&&<p><a href="/account">Open my account</a></p>}</main>}
